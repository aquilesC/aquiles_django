services:
  web:
    env_file:
      - ./env/.env.base
      - ./env/.env.staging
    restart: unless-stopped
    volumes:
      - ../.data/staging/static:/app/staticfiles
      - ../.data/staging/media:/app/media

  db:
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../.data/staging/backups:/backups
    restart: unless-stopped

  nginx:
    ports:
      - "80:80"
    restart: unless-stopped
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ../.data/staging/static:/var/www/static:ro
      - ../.data/staging/media:/var/www/media:ro
